/**
 * Flags produit / egress (aucune dépendance runtime externe).
 * `FF_SIMILAR_LISTINGS` : désactiver le bloc et les requêtes « annonces similaires » sur la fiche.
 */
export const FF_SIMILAR_LISTINGS = true;
