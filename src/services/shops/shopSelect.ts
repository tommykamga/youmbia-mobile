/** Colonnes publiques `shops` (lecture anon). */
export const SHOP_PUBLIC_SELECT =
  'id, owner_id, name, slug, status, description, logo_url, banner_url, whatsapp_phone, phone, city, is_verified, is_featured, created_at';

export const SHOP_SUMMARY_SELECT = 'id, slug, name, logo_url, is_verified, status';
